apiVersion: build.openshift.io/v1
kind: BuildConfig
metadata:
  name: backend-build
spec:
  source:
    type: Git
    git:
      uri: 'https://github.com/salmatarekx/EventPlanner.git'
      ref: phase3-testm
    contextDir: '/'
  strategy:
    type: Docker
    dockerStrategy:
      dockerfilePath: Dockerfile
  output:
    to:
      kind: ImageStreamTag
      name: 'eventplanner-backend:latest'
  triggers:
    - type: ConfigChange
    - type: GitHub
      github:
        secret: github-webhook-secret

---
apiVersion: image.openshift.io/v1
kind: ImageStream
metadata:
  name: eventplanner-backend
spec:
  lookupPolicy:
    local: false
